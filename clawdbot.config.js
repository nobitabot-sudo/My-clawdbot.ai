module.exports = {
  core: {
    llmProvider: 'google',
  },
  platforms: {
    telegram: {
      enabled: true,
      // Token hum already Environment Variables se le rahe hain
    },
    whatsapp: {
      enabled: true,
    }
  }
};
